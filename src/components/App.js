import styled from 'styled-components';
import DefaultPage from './DefaultPage';

const PageWrapper = styled.div``;

function App() {
    return (
        <PageWrapper>
            <DefaultPage />
        </PageWrapper>
    );
}

export default App;
